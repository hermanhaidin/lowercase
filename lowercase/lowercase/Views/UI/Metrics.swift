import SwiftUI

enum LCMetrics {
    /// Padding that visually aligns rounded controls with the screen bezel.
    static let bezelPadding: CGFloat = 34
    
    /// Default spacing between list sections when using `List`.
    static let listSectionSpacing: CGFloat = 8
}


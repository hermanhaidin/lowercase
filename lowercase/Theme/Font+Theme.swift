import SwiftUI

extension Font {
    /// Geist Pixel Square at body size, scaled for Dynamic Type.
    static let geistPixel = Font.custom(
        "GeistPixel-Square",
        size: 17,
        relativeTo: .body
    )
}
